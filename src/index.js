import React from "react"
import ReactDOM from "react-dom"

const App = () => {
 return (<h1>Testing Blog application, some changes from server as well</h1>)
}

ReactDOM.render(<App />, document.querySelector('#root'))
